#!/bin/bash
# $Id: chklnks,v 1.13 2016/04/14 11:47:03 fulford Exp $
# $Source: /src/merlin/usr/local/etc/RCS/chklnks,v $
# $Revision: 1.13 $
# Author C W Fulford.
# Copyright 2014 (c) C W Fulford.
# Licensed for public use under the LGPL, .
# For assistance contact fulford@fulford.net 0709 229 5385
########################################################################
cmd=`basename $0`
syntax="$cmd [-d] [-v]"
while [ $# -gt 0 ] ;do
	case $1 in 
		-d) set -x;debug=:;shift;;
		-r) action=delete;shift;;
		-v) verbose=0;shift;;
		-V) echo "$cmd $Revision: 1.13 $ $Date: 2016/04/14 11:47:03 $"|awk '{print $1,$3,$6}';exit;;  
		 /*) path="$path $2";shift;;
	esac
done
action=${action:-"echo"}
path=${path:-"./"}
echo $path
for d in $path;do
	if [ $verbose ];then
		echo "$cmd: checking directory $d"
	fi
	sudo find -L $d -type l -print|
	while read f ;do
		if [ $action == "delete" ];then
			if rm $f ;then 
				if [ $verbose ];then
					echo "$cmd: $f dead link removed" >&2
				fi
			else
				echo "$cmd: could not remove $f" >&2
			fi
		else
			$action "$cmd: found dead link $f" >&2
		fi
	done
done
